<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= produk.nama_produk %> - Detail Produk</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet"/>
  <style>
    body { font-family: 'Montserrat', sans-serif; }
  </style>
</head>
<body class="bg-white text-gray-900">
  <header class="border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16">
      <a class="flex items-center space-x-2" href="#">
        <img src="/img/logo.png" class="h-10 w-10 object-contain" alt="Logo">
        <span class="text-2xl font-bold text-indigo-600">stridecat Supply</span>
      </a>
      <nav class="hidden md:flex space-x-8 font-semibold text-gray-700">
        <a class="hover:text-indigo-600 transition" href="/">Home</a>
        <a class="hover:text-indigo-600 transition" href="/produk#products">Produk</a>
        <a class="hover:text-indigo-600 transition" href="/keranjang">Keranjang</a>
        <a class="hover:text-indigo-600 transition" href="/logout">Logout</a>
      </nav>
    </div>
  </header>

  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <div class="flex flex-col lg:flex-row lg:space-x-12">
      <div class="lg:w-1/2">
        <img src="<%= produk.foto %>" alt="<%= produk.nama_produk %>" class="w-full rounded-lg shadow-lg object-contain">
      </div>

      <div class="lg:w-1/2 mt-10 lg:mt-0">
        <h1 class="text-3xl font-extrabold text-gray-900"><%= produk.nama_produk %></h1>
        <p class="mt-2 text-indigo-600 font-semibold text-xl">Rp<%= produk.harga %></p>
        <p class="mt-4 text-gray-700"><%= produk.deskripsi %></p>
        <p class="mt-2 text-gray-700">Kategori: <%= produk.kategori %></p>
        <p class="mt-1 text-gray-700">Stok tersedia: <%= produk.jumlah_barang %> item</p>

        <form action="/keranjang/tambah" method="POST" class="mt-6">
          <input type="hidden" name="id_produk" value="<%= produk.id_produk %>">
          <button type="submit" class="mt-4 w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 rounded-md shadow-md transition">Tambah ke Keranjang</button>
        </form>
      </div>
    </div>
  </main>

  <footer class="bg-gray-100 border-t border-gray-200 mt-20">
    <div class="text-center text-gray-600 text-sm py-6">© 2024 My Vape Shop. All rights reserved.</div>
  </footer>
</body>
</html>
